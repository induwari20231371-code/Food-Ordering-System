# Food-Ordering-System
Backend API for Online Food Ordering System using Spring Boot, Spring Security with JWT, Spring Data JPA &amp; MySQL. Features user roles (ADMIN/CUSTOMER), cart operations, order processing &amp; payment tracking.
